import { useState, useEffect, useCallback } from 'react';
import { Leaf, Menu } from 'lucide-react';
import { Sidebar, type PageKey } from '@/components/Sidebar';
import { GlobalSearch } from '@/components/GlobalSearch';
import { Loading, ErrorState } from '@/components/States';
import { Dashboard } from '@/pages/Dashboard';
import { DossiersPage } from '@/pages/DossiersPage';
import { FicheDossier } from '@/pages/FicheDossier';
import { ClientsPage } from '@/pages/ClientsPage';
import { PaiementsPage } from '@/pages/PaiementsPage';
import { EcheancesPage } from '@/pages/EcheancesPage';
import { ExpertisesPage } from '@/pages/ExpertisesPage';
import { DocumentsPage } from '@/pages/DocumentsPage';
import { LaboratoirePage } from '@/pages/LaboratoirePage';
import { EnvironnementPage } from '@/pages/EnvironnementPage';
import { TopographieWithPlanPreviewLite } from '@/pages/TopographieWithPlanPreviewLite';
import { ParametresPage } from '@/pages/ParametresPage';
import { DossierForm } from '@/components/DossierForm';
import { AuthPage } from '@/pages/AuthPage';
import * as authService from '@/services/authService';
import type { Client,Dossier,DossierWithClient,Paiement,PaiementWithDossier,Expertise,ExpertiseWithDossier,DocumentRow,DocumentWithDossier,Laboratoire,Parametres,Session } from '@/types';
import * as data from '@/services/data';
function App(){
 const [session,setSession]=useState<Session|null>(()=>authService.getSession()); const [page,setPage]=useState<PageKey>('dashboard'); const [sidebarOpen,setSidebarOpen]=useState(false); const [loading,setLoading]=useState(true); const [error,setError]=useState('');
 const [clients,setClients]=useState<Client[]>([]),[dossiers,setDossiers]=useState<DossierWithClient[]>([]),[allPaiements,setAllPaiements]=useState<PaiementWithDossier[]>([]),[expertises,setExpertises]=useState<ExpertiseWithDossier[]>([]),[allDocuments,setAllDocuments]=useState<DocumentWithDossier[]>([]),[essais,setEssais]=useState<Laboratoire[]>([]),[parametres,setParametres]=useState<Parametres|null>(null);
 const [openDossierId,setOpenDossierId]=useState<string|null>(null),[dossierPaiements,setDossierPaiements]=useState<Paiement[]>([]),[dossierExpertise,setDossierExpertise]=useState<Expertise|null>(null),[dossierDocuments,setDossierDocuments]=useState<DocumentRow[]>([]),[editDossier,setEditDossier]=useState<Dossier|null>(null),[editFormOpen,setEditFormOpen]=useState(false);
 const loadAll=useCallback(async()=>{setLoading(true);setError('');try{const[c,d,p,e,docs,l,params]=await Promise.all([data.getClients(),data.getDossiers(),data.getAllPaiements(),data.getExpertises(),data.getAllDocuments(),data.getLaboratoire(),data.ensureParametres()]);setClients(c);setDossiers(d);setAllPaiements(p);setExpertises(e);setAllDocuments(docs);setEssais(l);setParametres(params);}catch(err){setError((err as Error).message)}finally{setLoading(false)}},[]);
 useEffect(()=>{loadAll()},[loadAll]);
 const openDossier=useCallback(async(id:string)=>{setOpenDossierId(id);setPage('dossiers');try{const[p,e,docs]=await Promise.all([data.getPaiementsByDossier(id),data.getExpertiseByDossier(id),data.getDocumentsByDossier(id)]);setDossierPaiements(p);setDossierExpertise(e);setDossierDocuments(docs)}catch(err){console.error(err)}},[]);
 const refreshDossierDetail=useCallback(async()=>{if(!openDossierId)return;try{const[p,e,docs]=await Promise.all([data.getPaiementsByDossier(openDossierId),data.getExpertiseByDossier(openDossierId),data.getDocumentsByDossier(openDossierId)]);setDossierPaiements(p);setDossierExpertise(e);setDossierDocuments(docs)}catch(err){console.error(err)}},[openDossierId]);
 const handleCreateDossier=async(d:Omit<Dossier,'id'|'created_at'|'numero'>)=>{await data.createDossier(d);await loadAll()}; const handleUpdateDossier=async(id:string,d:Partial<Dossier>)=>{await data.updateDossier(id,d);await loadAll();if(openDossierId===id)await refreshDossierDetail()}; const handleDeleteDossier=async(id:string)=>{await data.deleteDossier(id);setOpenDossierId(null);await loadAll()};
 const handleCreateClient=async(c:Omit<Client,'id'|'created_at'>)=>{await data.createClient(c);await loadAll()};const handleUpdateClient=async(id:string,c:Partial<Client>)=>{await data.updateClient(id,c);await loadAll()};const handleDeleteClient=async(id:string)=>{await data.deleteClient(id);await loadAll()};
 const handleCreatePaiement=async(p:Omit<Paiement,'id'|'created_at'>)=>{await data.createPaiement(p);await loadAll();await refreshDossierDetail()};const handleDeletePaiement=async(id:string)=>{await data.deletePaiement(id);await loadAll();await refreshDossierDetail()};
 const handleCreateDocument=async(d:Omit<DocumentRow,'id'|'created_at'>)=>{await data.createDocument(d);await loadAll();await refreshDossierDetail()};const handleDeleteDocument=async(id:string)=>{await data.deleteDocument(id);await loadAll();await refreshDossierDetail()};const handleUpdateDocumentPath=async(id:string,localPath:string)=>{await data.updateDocumentPath(id,localPath);await loadAll();await refreshDossierDetail()};const handleVerifyDocument=async(id:string,statut:string)=>{await data.updateDocumentStatut(id,statut);await loadAll();await refreshDossierDetail()};
 const handleCreateExpertise=async(e:Omit<Expertise,'id'|'created_at'|'numero'>)=>{await data.createExpertise(openDossierId?{...e,dossier_id:openDossierId}:e);await loadAll();await refreshDossierDetail()};const handleUpdateExpertise=async(id:string,e:Partial<Expertise>)=>{await data.updateExpertise(id,e);await loadAll();await refreshDossierDetail()};const handleDeleteExpertise=async(id:string)=>{await data.deleteExpertise(id);await loadAll();await refreshDossierDetail()};
 const handleCreateEssai=async(l:Omit<Laboratoire,'id'|'created_at'|'numero_essai'>)=>{await data.createLaboratoire(l);await loadAll()};const handleUpdateEssai=async(id:string,l:Partial<Laboratoire>)=>{await data.updateLaboratoire(id,l);await loadAll()};const handleDeleteEssai=async(id:string)=>{await data.deleteLaboratoire(id);await loadAll()};const handleSaveParametres=async(id:string,p:Partial<Parametres>)=>{await data.updateParametres(id,p);await loadAll()};
 const openDossierData=dossiers.find(d=>d.id===openDossierId)??null; const isEnvironmentDossier=openDossierData?.domaine==='Environnement';
 function renderPage(){if(loading)return <Loading message="Chargement des données..."/>;if(error)return <ErrorState message={error} onRetry={loadAll}/>;if(page==='environnement')return <EnvironnementPage clientName={openDossierData?.client?.nom??''} dossierNumero={openDossierData?.numero} initialPrestation={openDossierData?.prestation} onBack={()=>setPage('dossiers')}/>;if(page==='dossiers'&&openDossierId&&openDossierData)return <FicheDossier dossier={openDossierData} clients={clients} paiements={dossierPaiements} expertise={dossierExpertise} documents={dossierDocuments} onBack={()=>setOpenDossierId(null)} onEdit={d=>{setEditDossier(d);setEditFormOpen(true)}} onRefresh={refreshDossierDetail} onCreatePaiement={handleCreatePaiement} onDeletePaiement={handleDeletePaiement} onCreateDocument={handleCreateDocument} onDeleteDocument={handleDeleteDocument} onUpdateDocumentPath={handleUpdateDocumentPath} onVerifyDocument={handleVerifyDocument} onCreateExpertise={handleCreateExpertise} onUpdateExpertise={handleUpdateExpertise}/>;switch(page){case'dashboard':return <Dashboard dossiers={dossiers} paiements={allPaiements} onOpenDossier={openDossier}/>;case'dossiers':return <DossiersPage dossiers={dossiers} clients={clients} onOpenDossier={openDossier} onCreate={handleCreateDossier} onUpdate={handleUpdateDossier} onDelete={handleDeleteDossier}/>;case'clients':return <ClientsPage clients={clients} dossiers={dossiers} paiements={allPaiements} onOpenDossier={openDossier} onCreate={handleCreateClient} onUpdate={handleUpdateClient} onDelete={handleDeleteClient} onCreateDossier={handleCreateDossier} onUpdateDossier={handleUpdateDossier} onDeleteDossier={handleDeleteDossier}/>;case'documents':return <DocumentsPage documents={allDocuments} onOpenDossier={openDossier} onDelete={handleDeleteDocument} onUpdatePath={handleUpdateDocumentPath} onVerify={handleVerifyDocument}/>;case'paiements':return <PaiementsPage paiements={allPaiements} dossiers={dossiers} clients={clients} onOpenDossier={openDossier} onCreatePaiement={handleCreatePaiement} onDeletePaiement={handleDeletePaiement}/>;case'echeances':return <EcheancesPage dossiers={dossiers} onOpenDossier={openDossier}/>;case'expertises':return <ExpertisesPage expertises={expertises} onOpenDossier={openDossier} onCreate={handleCreateExpertise} onUpdate={handleUpdateExpertise} onDelete={handleDeleteExpertise}/>;case'laboratoire':return <LaboratoirePage essais={essais} onCreate={handleCreateEssai} onUpdate={handleUpdateEssai} onDelete={handleDeleteEssai}/>;case'topographie':return <TopographieWithPlanPreviewLite/>;case'parametres':return <ParametresPage parametres={parametres} onSave={handleSaveParametres}/>;default:return null}}
 if(!session)return <AuthPage onAuth={s=>setSession(s)}/>;return <div className="flex h-screen bg-gray-50 overflow-hidden"><Sidebar current={page} onNavigate={p=>{setPage(p);if(p!=='dossiers'&&p!=='environnement')setOpenDossierId(null)}} open={sidebarOpen} onClose={()=>setSidebarOpen(false)} session={session} onLogout={()=>{authService.logout();setSession(null)}}/><div className="flex-1 flex flex-col overflow-hidden"><header className="bg-white border-b border-gray-200 px-4 py-3 flex items-center gap-3 flex-shrink-0"><button onClick={()=>setSidebarOpen(true)} className="lg:hidden p-2 rounded-lg hover:bg-gray-100 text-gray-600"><Menu size={20}/></button><GlobalSearch dossiers={dossiers} onSelectDossier={openDossier}/>{isEnvironmentDossier&&page==='dossiers'&&<button onClick={()=>setPage('environnement')} className="hidden sm:inline-flex items-center gap-2 px-3 py-2 rounded-lg bg-emerald-50 text-emerald-700 border border-emerald-200 text-xs font-semibold hover:bg-emerald-100"><Leaf size={15}/>Module Environnement</button>}{dossiers.length>0&&allPaiements.length===0&&<span className="hidden md:inline-block text-xs text-amber-600 bg-amber-50 px-2 py-1 rounded-full border border-amber-200">Données de démonstration</span>}</header><main className="flex-1 overflow-y-auto p-4 lg:p-6">{renderPage()}</main></div><DossierForm open={editFormOpen} onClose={()=>setEditFormOpen(false)} onSave={async d=>{if(editDossier)await handleUpdateDossier(editDossier.id,d);setEditFormOpen(false)}} clients={clients} initial={editDossier}/></div>}
export default App;
