diff --git a/src/types/index.ts b/src/types/index.ts
--- a/src/types/index.ts
+++ b/src/types/index.ts
@@ -196,6 +196,7 @@ export const ETAT_PIECES: EtatPieces[] = ['Complet', 'Incomplet', 'Manquant'];
 export interface Client {
   id: string;
   nom: string;
+  prenom: string | null;
   telephone: string | null;
   email: string | null;
   adresse: string | null;
 diff --git a/src/pages/ClientsPage.tsx b/src/pages/ClientsPage.tsx
--- a/src/pages/ClientsPage.tsx
+++ b/src/pages/ClientsPage.tsx
@@ -117,6 +117,7 @@ export function ClientsPage({
                     >
                       {c.nom}
                     </h3>
+                    {c.prenom && <p className="text-xs text-gray-500">{c.prenom}</p>}
                   </div>
                 </div>
@@ -459,6 +460,7 @@ function ClientForm({
   const [form, setForm] = useState({
     nom: '',
+    prenom: '',
     telephone: '',
     email: '',
     adresse: '',
@@ -478,6 +480,7 @@ function ClientForm({
       setForm({
         nom: initial.nom,
+        prenom: initial.prenom ?? '',
         telephone: initial.telephone ?? '',
         email: initial.email ?? '',
         adresse: initial.adresse ?? '',
@@ -493,6 +496,7 @@ function ClientForm({
       setForm({
         nom: '',
+        prenom: '',
         telephone: '',
         email: '',
         adresse: '',
@@ -523,6 +527,7 @@ function ClientForm({
       await onSave({
         nom: form.nom.trim(),
+        prenom: form.prenom.trim() || null,
         telephone: form.telephone || null,
         email: form.email || null,
         adresse: form.adresse || null,
@@ -551,6 +556,14 @@ function ClientForm({
         <Field label="Nom / Raison sociale" required>
           <input
             className={inputCls}
             value={form.nom}
             onChange={(e) => setForm({ ...form, nom: e.target.value })}
           />
         </Field>
+        <Field label="Prénom">
+          <input
+            className={inputCls}
+            value={form.prenom}
+            onChange={(e) => setForm({ ...form, prenom: e.target.value })}
+          />
+        </Field>
         <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
@@ -662,6 +675,10 @@ function ClientDetail({
           <div className="grid grid-cols-1 md:grid-cols-2 gap-3 text-sm">
+            {client.prenom && (
+              <div>
+                <p className="text-xs font-semibold text-gray-500">Prénom</p>
+                <p className="text-gray-800">{client.prenom}</p>
+              </div>
+            )}
             {client.telephone && (
               <div>
                 <p className="text-xs font-semibold text-gray-500">Téléphone</p>
 diff --git a/src/components/Demande4300Page.tsx b/src/components/Demande4300Page.tsx
--- a/src/components/Demande4300Page.tsx
+++ b/src/components/Demande4300Page.tsx
@@ -35,7 +35,7 @@ export function Demande4300Page({clients,onUpdateClient,onClose,parcel,parcelAdmin
  const save=async()=>{if(!client)return;if(!form.nom?.trim()){setMessage('Le nom du client est obligatoire.');return}setSaving(true);setMessage('');try{await onUpdateClient(client.id,form as Partial<Client>);setMessage('Informations du client enregistrées.')}catch(e){setMessage(`Erreur : ${(e as Error).message}`)}finally{setSaving(false)}};
@@
-  <div className="rounded-xl border bg-white p-5 shadow-sm"><div className="mb-5 flex items-center gap-2 border-b pb-4"><UserRound size={20} className="text-sky-600"/><h2 className="font-semibold">DEMANDEUR — Client</h2></div>{clients.length===0?<div className="rounded-lg bg-amber-50 p-4 text-sm">Aucun client enregistré dans le module Clients.</div>:<><div className="mb-5 max-w-xl"><label className="mb-1 block text-sm font-medium">Sélectionner le client</label><select value={clientId} onChange={e=>setClientId(e.target.value)} className="w-full rounded-lg border px-3 py-2 text-sm">{clients.map(c=><option key={c.id} value={c.id}>{c.nom}</option>)}</select></div><div className="grid grid-cols-1 gap-4 md:grid-cols-2">{clientFields.map(([k,l])=><div key={k} className={k==='observations'?'md:col-span-2':''}><label className="mb-1 block text-sm font-medium">{l}</label>{k==='observations'?<textarea value={form[k]??''} onChange={e=>setValue(k,e.target.value)} rows={3} className="w-full rounded-lg border px-3 py-2 text-sm"/>:<input value={form[k]??''} onChange={e=>setValue(k,e.target.value)} className="w-full rounded-lg border px-3 py-2 text-sm"/>}</div>)}</div></>}</div>
+  <div className="rounded-xl border bg-white p-5 shadow-sm"><div className="mb-5 flex items-center gap-2 border-b pb-4"><UserRound size={20} className="text-sky-600"/><h2 className="font-semibold">DEMANDEUR — Client</h2></div>{clients.length===0?<div className="rounded-lg bg-amber-50 p-4 text-sm">Aucun client enregistré dans le module Clients.</div>:<><div className="mb-5 max-w-xl"><label className="mb-1 block text-sm font-medium">Sélectionner le client</label><select value={clientId} onChange={e=>setClientId(e.target.value)} className="w-full rounded-lg border px-3 py-2 text-sm">{clients.map(c=><option key={c.id} value={c.id}>{c.nom}{c.prenom ? ` — ${c.prenom}` : ''}</option>)}</select></div><div className="grid grid-cols-1 gap-4 md:grid-cols-2">{clientFields.map(([k,l])=><div key={k} className={k==='observations'?'md:col-span-2':''}><label className="mb-1 block text-sm font-medium">{l}</label>{k==='observations'?<textarea value={form[k]??''} onChange={e=>setValue(k,e.target.value)} rows={3} className="w-full rounded-lg border px-3 py-2 text-sm"/>:<input value={form[k]??''} onChange={e=>setValue(k,e.target.value)} className="w-full rounded-lg border px-3 py-2 text-sm"/>}</div>)}</div></>}</div>
